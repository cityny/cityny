name: Update Repos
on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/blog-post-workflow@v1
        with:
          comment_tag_name: "RECENT_REPOS"
          feed_list: "https://github.com/cityny.atom"
          max_post_count: 5
          # Usamos las variables de la documentación que compartiste:
          template: "$randomEmoji(🚀,🛠️,📱) [$title]($url) $newline"
          # Esta opción es vital para que no se vea el código HTML:
          description_max_length: 1 
          # Evita que el HTML rompa el diseño:
          disable_html_encoding: false
