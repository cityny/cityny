name: Update Repos
on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/blog-post-workflow@v1
        with:
          comment_tag_name: "RECENT_REPOS"
          # CAMBIO CLAVE: Esta URL solo muestra actividad de creación/repos
          feed_list: "https://github.com/cityny?tab=repositories&q=&type=public&language=&sort=stargazers.atom"
          max_post_count: 5
          template: "$randomEmoji(🚀,🛠️,📱) [$title]($url) $newline"
          description_max_length: 1
